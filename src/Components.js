import { styled } from '@mui/system'
import TextField from '@mui/material/TextField';
import Button from '@mui/material/Button';

export const TimerButton = styled(Button)({
  color: 'gold',
  backgroundColor: '#333333',
  padding: 8,
  borderRadius: 24,
  // width: 400,
  height: 400,
  fontSize: 400
});
export const HandTextField = styled(TextField)({
  color: 'gold',
  width: 500,
  height: 400,
})
export const TableTextField = styled(TextField)({
  color: 'gold',
  width: 300,
  height: 400,
})
export const SeatTextField = styled(TextField)({
  color: 'gold',
  width: 300,
  height: 400,
})


